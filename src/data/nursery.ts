import {
  DEFAULT_STAT_GRADES,
  STAT_KEYS,
  applyStatGrades,
  getBaseMaxHearts,
  getCreatureMaxEnergyFromStats,
  getHabitatIdForFamily,
  getSpeciesDefinition,
  getVariantDefinition,
  getVariantsForFamily,
  rollStatGrades,
  shiftStatGrade,
} from "@/data/creatures";
import { getRanchUpgradeEffects, getRanchUpgrades } from "@/data/ranchUpgrades";
import type { BreedingParticipant } from "@/types/breeding";
import type { CreatureAbility, CreatureLineageRisk, CreatureRecord, CreatureStatKey, CreatureStats, StatGrade, StatGrades } from "@/types/creature";
import type { CreatureId, EggId, PregnancyId, SpeciesId, VariantId } from "@/types/ids";
import type { EggRecord, GameSave, InheritancePreview, ParentSnapshot, PregnancyRecord } from "@/types/save";

export const NURSERY_ASSETS = { egg: "/images/ui/icons/icon_egg.png", pregnancy: "/images/ui/icons/icon_pregnancy.png", timer: "/images/ui/icons/icon_timer_hourglass.png", hatch: "/images/ui/icons/icon_hatch.png", background: "/images/backgrounds/nursery/egg_nursery_interior.png", originHatched: "/images/ui/icons/icon_origin_hatched.png", parentCompare: "/images/ui/icons/icon_parent_compare.png", statGrade: "/images/ui/icons/icon_stat_grade.png" } as const;

const RISK_LABELS: Record<CreatureLineageRisk, string> = { none: "No Risk", "half-sibling": "Half Sibling Line", "full-sibling": "Full Sibling Line", "parent-child": "Direct Lineage" };
const RISK_GRADE_PENALTY_CHANCE: Record<CreatureLineageRisk, number> = { none: 0, "half-sibling": 16, "full-sibling": 28, "parent-child": 36 };
const RISK_STAT_PENALTY_CHANCE: Record<CreatureLineageRisk, number> = { none: 0, "half-sibling": 14, "full-sibling": 24, "parent-child": 32 };
const RISK_UPSIDE_CHANCE: Record<CreatureLineageRisk, number> = { none: 0, "half-sibling": 3, "full-sibling": 5, "parent-child": 7 };
const RISK_FRAGILE_CHANCE: Record<CreatureLineageRisk, number> = { none: 0, "half-sibling": 2, "full-sibling": 4, "parent-child": 6 };
const BREEDING_ABILITY_INHERIT_CHANCE_BY_TIER = [16, 22, 30, 40, 52];
const BREEDING_SECOND_ABILITY_CHANCE_BY_TIER = [0, 2, 5, 9, 15];
const BREEDING_NEW_ABILITY_CHANCE_BY_TIER = [1, 1, 2, 3, 5];
const RISK_STAT_KEYS: CreatureStatKey[] = ["FER", "STA", "WIL"];
const GENERIC_NAME_WORDS = new Set(["base", "common", "uncommon", "rare", "epic", "hatchling", "feline", "canine", "bovine", "lapine", "equine", "cow", "horse", "bunny"]);

type NameFamily = "feline" | "canine" | "bovine" | "lapine" | "equine";
type NamePool = { prefixes: readonly string[]; cores: readonly string[]; suffixes: readonly string[]; fallbackNames: readonly string[] };

const FAMILY_NAME_POOLS: Record<NameFamily, NamePool> = {
  feline: { prefixes: ["mi", "ny", "vel", "sai", "ari", "ka", "lia", "se", "no", "vessa", "mara", "zai", "elo", "kiri", "syl", "tavi", "nira", "lu", "fira", "yara"], cores: ["ra", "lyn", "mir", "purr", "vessa", "sable", "whisk", "mew", "shade", "vel", "glim", "silk", "dawn", "ember", "rill", "cinder", "moth", "lume", "soft", "tail"], suffixes: ["a", "ia", "elle", "ira", "lyn", "ari", "ora", "wyn", "el", "in", "essa", "ae", "ina", "ette", "yra", "is", "ara", "ith", "una", "li"], fallbackNames: ["Mirae", "Velin", "Nyra", "Sable", "Arielle", "Liora", "Kiva", "Selia", "Vessa", "Nimue", "Yariel", "Tavia", "Kirin", "Marlia", "Zaira", "Fiora", "Luneth", "Silka", "Mirelle", "Nyris", "Velora", "Ariwyn", "Mevia", "Soril", "Calira", "Elyra", "Minala", "Pavelle", "Rylia", "Oriana"] },
  canine: { prefixes: ["ro", "ka", "brin", "fen", "tav", "dor", "ruk", "valo", "koro", "ash", "gar", "tor", "wulf", "bry", "ced", "or", "thane", "ren", "dax", "moro"], cores: ["ook", "rusk", "howl", "guard", "valo", "brave", "fang", "trail", "pack", "loyal", "ridge", "bark", "iron", "swift", "watch", "ember", "oak", "storm", "roam", "flint"], suffixes: ["en", "or", "a", "ik", "wyn", "ro", "an", "os", "ra", "in", "eth", "ar", "o", "us", "ell", "ric", "on", "is", "ak", "yr"], fallbackNames: ["Rookin", "Brindle", "Fenro", "Tavon", "Korra", "Dorin", "Rusk", "Valo", "Kairo", "Thane", "Daxen", "Moro", "Garik", "Bryn", "Fennor", "Kavik", "Cedric", "Orwyn", "Rugar", "Ashor", "Torin", "Renko", "Flint", "Barko", "Wulfen", "Daro", "Kodan", "Varric", "Oskar", "Rollo"] },
  bovine: { prefixes: ["bru", "bel", "dun", "mara", "terra", "cal", "clo", "moss", "bryn", "dara", "flora", "glen", "havi", "ora", "nella", "pra", "sova", "til", "bar", "hol"], cores: ["una", "mead", "clove", "bell", "graze", "moss", "field", "cream", "hearth", "pasture", "barley", "daisy", "milk", "brook", "plum", "hazel", "loam", "root", "cedar", "honey"], suffixes: ["a", "elle", "wyn", "ra", "na", "ora", "ia", "lin", "eth", "bel", "una", "ess", "i", "el", "ara", "or", "in", "ette", "lyn", "ola"], fallbackNames: ["Brunae", "Talla", "Bellora", "Marn", "Clovelle", "Duna", "Marabel", "Terra", "Calora", "Daisy", "Brynna", "Glenna", "Havina", "Nella", "Sovia", "Barley", "Hazel", "Clovia", "Mosselle", "Florin", "Ora", "Prairie", "Bramble", "Darleen", "Marlow", "Loama", "Cedra", "Honeyra", "Plumelle", "Hollyn"] },
  lapine: { prefixes: ["pi", "ta", "luna", "bri", "nib", "syl", "bun", "cotta", "fenn", "miri", "pella", "rini", "sora", "thim", "willa", "zini", "hop", "nella", "oro", "vivi"], cores: ["pip", "bur", "moss", "quick", "sprig", "moon", "thump", "clover", "fern", "dandel", "meadow", "soft", "skip", "petal", "burrow", "seed", "dew", "mint", "rush", "bloom"], suffixes: ["a", "i", "elle", "kin", "wyn", "ra", "ie", "ina", "ora", "lyn", "ette", "y", "is", "ella", "en", "li", "ara", "o", "wynne", "ia"], fallbackNames: ["Pippa", "Talla", "Pipkin", "Lunelle", "Bria", "Sylvie", "Nibbia", "Clover", "Fennel", "Miri", "Pella", "Rini", "Sorrel", "Thimble", "Willa", "Zinnie", "Hopkin", "Nella", "Orella", "Vivi", "Dandel", "Petra", "Meadow", "Burwyn", "Minty", "Bloomie", "Skippa", "Dewra", "Fernelle", "Spriggy"] },
  equine: { prefixes: ["mar", "el", "astra", "vel", "can", "sol", "briar", "dawn", "equi", "fara", "gale", "her", "ivan", "jora", "kael", "luma", "mira", "nova", "or", "sable"], cores: ["low", "mane", "gait", "field", "star", "stride", "hoof", "canter", "rider", "meadow", "wind", "silver", "bramble", "dash", "sun", "trail", "crest", "river", "haze", "oak"], suffixes: ["o", "a", "wyn", "elle", "ian", "ra", "or", "is", "en", "ia", "ara", "eth", "in", "el", "ora", "ius", "yn", "al", "eon", "li"], fallbackNames: ["Marlowen", "Astral", "Canter", "Velora", "Solwyn", "Elian", "Briar", "Dawnelle", "Faralon", "Gale", "Ivanor", "Jorah", "Kaelin", "Lumara", "Novelle", "Orion", "Sable", "Strider", "Cresta", "Riveron", "Haze", "Oakley", "Meadowyn", "Silvara", "Dasha", "Trailyn", "Starra", "Hoofen", "Canelle", "Astrae"] },
};

const VARIANT_FLAVOR_POOLS: Array<{ keywords: readonly string[]; pool: NamePool }> = [
  { keywords: ["dream", "moon", "lop"], pool: { prefixes: ["luna", "sel", "drea", "mira", "nox", "soma", "vela", "liri", "moru", "ayla"], cores: ["veil", "moon", "soft", "slumber", "glow", "hush", "mist", "silver", "drowse", "wish"], suffixes: ["elle", "ira", "wyn", "lia", "ora", "eth", "a", "is", "ina", "ae"], fallbackNames: ["Lunelle", "Mirae", "Selwyn", "Vesper", "Drealia", "Somira", "Ayla", "Velora", "Lirielle", "Moruin"] } },
  { keywords: ["night", "mare", "shadow"], pool: { prefixes: ["nox", "ves", "umb", "nyx", "sha", "dusk", "morn", "raven", "valo", "noir"], cores: ["shade", "gloam", "dusk", "raven", "vel", "night", "thorn", "smoke", "ash", "echo"], suffixes: ["ra", "en", "wyn", "elle", "ix", "or", "a", "is", "eth", "yn"], fallbackNames: ["Vesper", "Nyxen", "Umbra", "Duskelle", "Velrix", "Noira", "Ravenor", "Ashwyn", "Echo", "Thorna"] } },
  { keywords: ["unicorn", "star", "horn"], pool: { prefixes: ["astra", "cel", "lum", "iv", "sol", "pria", "nova", "halo", "ely", "aur"], cores: ["star", "gleam", "horn", "silver", "prism", "ray", "crown", "glint", "dawn", "shine"], suffixes: ["ia", "elle", "wyn", "ora", "ian", "a", "is", "el", "ara", "eth"], fallbackNames: ["Asteria", "Celwyn", "Lumielle", "Ivoran", "Solara", "Novara", "Halion", "Auriel", "Prisma", "Elyra"] } },
  { keywords: ["yak", "tundra", "snow"], pool: { prefixes: ["moro", "luna", "brum", "tund", "ora", "frost", "bar", "yara", "kora", "nive"], cores: ["snow", "moss", "horn", "drift", "stone", "lichen", "ridge", "wool", "peak", "fir"], suffixes: ["a", "or", "wyn", "ra", "elle", "en", "ia", "ak", "eth", "is"], fallbackNames: ["Morwyn", "Brumora", "Lunara", "Tundell", "Stonea", "Nivea", "Korak", "Yarrow", "Frostin", "Peakra"] } },
  { keywords: ["minotaur", "maze", "forge"], pool: { prefixes: ["tor", "brak", "min", "daro", "kael", "gor", "stone", "bar", "vorn", "karn"], cores: ["horn", "stone", "forge", "maze", "guard", "iron", "ember", "wall", "anvil", "oath"], suffixes: ["an", "or", "a", "ek", "ra", "os", "in", "eth", "ar", "um"], fallbackNames: ["Torren", "Brakor", "Kaelra", "Minora", "Darek", "Goran", "Vorn", "Karnum", "Anvilar", "Oathen"] } },
  { keywords: ["antler", "hare", "thicket"], pool: { prefixes: ["hart", "bri", "vel", "sprig", "fawn", "hazel", "thorn", "moss", "glen", "syl"], cores: ["antler", "moss", "quick", "thicket", "burrow", "fern", "branch", "clover", "rush", "briar"], suffixes: ["a", "wyn", "elle", "kin", "ra", "en", "ia", "is", "lyn", "eth"], fallbackNames: ["Hartley", "Briwyn", "Sprigga", "Fawna", "Thicket", "Hazelle", "Thornby", "Glenna", "Sylkin", "Mossra"] } },
];

function getCreatureXpToNext(level: number): number { return 45 + level * 30; }
function deterministicRoll(seed: string, modulo = 100): number { let hash = 0; for (let index = 0; index < seed.length; index += 1) hash = (hash * 31 + seed.charCodeAt(index)) % 1000003; return Math.abs(hash) % modulo; }
function getHabitatIdForSpecies(speciesId: SpeciesId) { return getHabitatIdForFamily(getSpeciesDefinition(speciesId).family); }
function parentSnapshot(participant: BreedingParticipant): ParentSnapshot { return { participantId: participant.participantId, creatureId: participant.creatureId, displayName: participant.displayName, familyLabel: participant.familyLabel, kind: participant.kind }; }
function averageStat(statKey: keyof CreatureStats, giver?: CreatureRecord, receiver?: CreatureRecord): number { const values = [giver?.stats[statKey], receiver?.stats[statKey]].filter((value): value is number => typeof value === "number"); return values.length ? Math.round(values.reduce((sum, value) => sum + value, 0) / values.length) : 5; }
function cleanNameSource(name: string, fallback: string): string { const words = name.replace(/[^a-zA-Z\s]/g, " ").split(/\s+/).filter(Boolean).filter((word) => !GENERIC_NAME_WORDS.has(word.toLowerCase())); return (words.join("") || fallback).replace(/[^a-zA-Z]/g, "").toLowerCase(); }
function titleCaseName(value: string): string { return value ? value.slice(0, 1).toUpperCase() + value.slice(1).toLowerCase() : "Hatchling"; }
function normalizeNameFamily(value: string): NameFamily { if (value === "canine" || value === "bovine" || value === "lapine" || value === "equine") return value; return "feline"; }
function pickFrom<T>(items: readonly T[], seed: string, fallback: T): T { return items[deterministicRoll(seed, items.length || 1)] ?? fallback; }
function mergeNamePools(familyPool: NamePool, flavorPool?: NamePool): NamePool { return { prefixes: [...(flavorPool?.prefixes ?? []), ...familyPool.prefixes], cores: [...(flavorPool?.cores ?? []), ...familyPool.cores], suffixes: [...(flavorPool?.suffixes ?? []), ...familyPool.suffixes], fallbackNames: [...(flavorPool?.fallbackNames ?? []), ...familyPool.fallbackNames, "Mirae", "Talla", "Brindle", "Velin", "Pippa", "Lunelle", "Solwyn", "Asteria"] }; }
function getFlavorPool(variantName: string, speciesName: string, rarity: string): NamePool | undefined { const text = `${variantName} ${speciesName} ${rarity}`.toLowerCase(); return VARIANT_FLAVOR_POOLS.find((flavor) => flavor.keywords.some((keyword) => text.includes(keyword)))?.pool; }
function parentEcho(source: string, seed: string): string { if (!source) return ""; if (source.length <= 2) return source; const options = [source.slice(0, 2), source.slice(Math.max(0, source.length - 2)), `${source[0]}${source[source.length - 1]}`, source.slice(0, 1)]; return pickFrom(options.filter((option) => option.length >= 1), seed, source.slice(0, 1)); }
function softenNameCandidate(value: string): string { return value.toLowerCase().replace(/[^a-z]/g, "").replace(/([a-z])\1{2,}/g, "$1$1").slice(0, 10); }
function isGoodHatchlingName(candidate: string, giver: string, receiver: string): boolean { if (candidate.length < 4 || candidate.length > 10) return false; if (!/[aeiouy]/.test(candidate)) return false; if (GENERIC_NAME_WORDS.has(candidate)) return false; const directBlends = [`${giver}${receiver}`, `${receiver}${giver}`].filter((blend) => blend.length >= 4); if (directBlends.some((blend) => candidate === blend || candidate.includes(blend))) return false; if (giver.length > 2 && receiver.length > 2 && candidate.includes(giver) && candidate.includes(receiver)) return false; if (/(.)\1\1/.test(candidate)) return false; return true; }
export function getLineageRiskLabel(risk?: CreatureLineageRisk): string { return RISK_LABELS[risk ?? "none"] ?? RISK_LABELS.none; }
export function suggestHatchlingName(egg: Pick<EggRecord, "parents" | "variantId" | "speciesId" | "eggId">): string { const variant = getVariantDefinition(egg.variantId); const species = getSpeciesDefinition(egg.speciesId); const familyPool = FAMILY_NAME_POOLS[normalizeNameFamily(variant.family)]; const flavorPool = getFlavorPool(variant.name, species.name, variant.rarity); const pool = mergeNamePools(familyPool, flavorPool); const giver = cleanNameSource(egg.parents.giver.displayName, variant.name); const receiver = cleanNameSource(egg.parents.receiver.displayName, species.name); const primaryParent = deterministicRoll(`${egg.eggId}_name_parent`, 2) === 0 ? giver : receiver; const secondaryParent = primaryParent === giver ? receiver : giver; const primaryEcho = parentEcho(primaryParent, `${egg.eggId}_name_primary_echo`); const secondaryEcho = parentEcho(secondaryParent, `${egg.eggId}_name_secondary_echo`); const prefix = pickFrom(pool.prefixes, `${egg.eggId}_name_prefix`, "mi"); const core = pickFrom(pool.cores, `${egg.eggId}_name_core`, "ra"); const suffix = pickFrom(pool.suffixes, `${egg.eggId}_name_suffix`, "a"); const familySuffix = pickFrom(familyPool.suffixes, `${egg.eggId}_name_family_suffix`, "a"); const flavorPrefix = flavorPool ? pickFrom(flavorPool.prefixes, `${egg.eggId}_name_flavor_prefix`, prefix) : prefix; const candidates = [`${prefix}${core}${suffix}`, `${flavorPrefix}${primaryEcho}${suffix}`, `${primaryEcho}${core}${familySuffix}`, `${prefix}${secondaryEcho}${suffix}`, `${core}${primaryEcho}${familySuffix}`, `${flavorPrefix}${core}${familySuffix}`, ...pool.fallbackNames.map((name) => name.toLowerCase())].map(softenNameCandidate); const chosen = candidates.find((candidate) => isGoodHatchlingName(candidate, giver, receiver)) ?? softenNameCandidate(pickFrom(pool.fallbackNames, `${egg.eggId}_name_fallback`, "Mirae")); return titleCaseName(chosen || "Hatchling"); }

function getParentIds(creature?: CreatureRecord): CreatureId[] { return creature?.lineage?.parentCreatureIds ?? []; }
function hasParent(creature: CreatureRecord | undefined, parentId: CreatureId | undefined): boolean { return Boolean(creature && parentId && getParentIds(creature).includes(parentId)); }
function sharedParentCount(giver?: CreatureRecord, receiver?: CreatureRecord): number { const giverParents = new Set(getParentIds(giver)); return getParentIds(receiver).filter((id) => giverParents.has(id)).length; }
function analyzeLineage(giver?: CreatureRecord, receiver?: CreatureRecord): { risk: CreatureLineageRisk; label: string; notes: string[] } { if (!giver || !receiver) return { risk: "none", label: RISK_LABELS.none, notes: ["Lineage risk is low because one contributor is not a tracked creature."] }; if (hasParent(giver, receiver.creatureId) || hasParent(receiver, giver.creatureId)) return { risk: "parent-child", label: RISK_LABELS["parent-child"], notes: ["Direct lineage detected. Small fertility, stamina, and willpower penalties are more likely, but rare positive mutations can still occur."] }; const shared = sharedParentCount(giver, receiver); if (shared >= 2) return { risk: "full-sibling", label: RISK_LABELS["full-sibling"], notes: ["Full sibling line detected. The hatchling has elevated risk of weaker FER, STA, or WIL outcomes."] }; if (shared === 1) return { risk: "half-sibling", label: RISK_LABELS["half-sibling"], notes: ["Half sibling line detected. The penalty risk is mild, but the lineage is marked for tracking."] }; return { risk: "none", label: RISK_LABELS.none, notes: ["No close family risk detected from tracked contributors."] }; }

function inheritGradeForStat(seed: string, statKey: keyof CreatureStats, giver?: CreatureRecord, receiver?: CreatureRecord): { grade: StatGrade; note: string | null } { const parentGrades = [giver?.statGrades?.[statKey], receiver?.statGrades?.[statKey]].filter((grade): grade is StatGrade => Boolean(grade)); const fallbackGrade = rollStatGrades(`${seed}_fallback`, "Common")[statKey]; const inheritedBase = parentGrades.length ? parentGrades[deterministicRoll(`${seed}_${statKey}_parent`, parentGrades.length)] : fallbackGrade; const roll = deterministicRoll(`${seed}_${statKey}_shift`); if (roll >= 96) { const upgraded = shiftStatGrade(inheritedBase, 1); return { grade: upgraded, note: upgraded === inheritedBase ? `${statKey} inherited ${inheritedBase}.` : `${statKey} inherited ${inheritedBase} and rarely upgraded to ${upgraded}.` }; } if (roll <= 7) { const downgraded = shiftStatGrade(inheritedBase, -1); return { grade: downgraded, note: downgraded === inheritedBase ? `${statKey} inherited ${inheritedBase}.` : `${statKey} inherited ${inheritedBase} but downgraded to ${downgraded}.` }; } return { grade: inheritedBase, note: `${statKey} inherited ${inheritedBase}.` }; }
function inheritStatGrades(seed: string, giver?: CreatureRecord, receiver?: CreatureRecord): { statGrades: StatGrades; notes: string[] } { const notes: string[] = []; const statGrades = STAT_KEYS.reduce((grades, key) => { const inherited = inheritGradeForStat(seed, key, giver, receiver); if (inherited.note) notes.push(inherited.note); return { ...grades, [key]: inherited.grade }; }, {} as StatGrades); return { statGrades, notes }; }
function applyLineageRiskToGrades(seed: string, grades: StatGrades, risk: CreatureLineageRisk): { statGrades: StatGrades; notes: string[]; traits: string[] } { let nextGrades = { ...grades }; const notes: string[] = []; const traits: string[] = []; const penaltyChance = RISK_GRADE_PENALTY_CHANCE[risk]; RISK_STAT_KEYS.forEach((key) => { if (penaltyChance && deterministicRoll(`${seed}_lineage_grade_${key}`, 100) < penaltyChance) { const downgraded = shiftStatGrade(nextGrades[key], -1); if (downgraded !== nextGrades[key]) { notes.push(`${key} grade was softened by ${getLineageRiskLabel(risk)} pressure: ${nextGrades[key]} → ${downgraded}.`); nextGrades = { ...nextGrades, [key]: downgraded }; } } }); const upsideChance = RISK_UPSIDE_CHANCE[risk]; if (upsideChance && deterministicRoll(`${seed}_lineage_upside`, 100) < upsideChance) { const key = STAT_KEYS[deterministicRoll(`${seed}_lineage_upside_stat`, STAT_KEYS.length)]; const upgraded = shiftStatGrade(nextGrades[key], 1); if (upgraded !== nextGrades[key]) { notes.push(`Risky pairing produced a rare stabilizing mutation: ${key} grade ${nextGrades[key]} → ${upgraded}.`); nextGrades = { ...nextGrades, [key]: upgraded }; } } if (RISK_FRAGILE_CHANCE[risk] && deterministicRoll(`${seed}_fragile_lineage`, 100) < RISK_FRAGILE_CHANCE[risk]) { traits.push("Fragile Lineage"); notes.push("Fragile Lineage marker appeared. This is rare and will be tracked on the creature profile."); } return { statGrades: nextGrades, notes, traits }; }
function buildInheritedStats(seed: string, baseStats: CreatureStats, statGrades: StatGrades, giver?: CreatureRecord, receiver?: CreatureRecord): { stats: CreatureStats; notes: string[] } { const notes: string[] = []; const rawStats = STAT_KEYS.reduce((nextStats, statKey, index) => { const inheritedAverage = averageStat(statKey, giver, receiver); const gradeAdjustedBase = applyStatGrades(baseStats, statGrades)[statKey]; const baseAnchor = Math.round((inheritedAverage + gradeAdjustedBase) / 2); const roll = deterministicRoll(`${seed}_${statKey}_${index}`, 100); let variance = 0; if (roll <= 7) { variance = -2; notes.push(`${statKey} rolled unusually low.`); } else if (roll <= 24) variance = -1; else if (roll >= 96) { variance = 2; notes.push(`${statKey} rolled unusually high.`); } else if (roll >= 78) variance = 1; return { ...nextStats, [statKey]: Math.max(1, baseAnchor + variance) }; }, {} as CreatureStats); if (!notes.length) notes.push("Stats inherited normally from parent averages, stat grades, and minor RNG."); return { stats: rawStats, notes }; }
function applyLineageRiskToStats(seed: string, stats: CreatureStats, risk: CreatureLineageRisk): { stats: CreatureStats; notes: string[] } { let nextStats = { ...stats }; const notes: string[] = []; const chance = RISK_STAT_PENALTY_CHANCE[risk]; RISK_STAT_KEYS.forEach((key) => { if (chance && deterministicRoll(`${seed}_lineage_stat_${key}`, 100) < chance) { nextStats = { ...nextStats, [key]: Math.max(1, nextStats[key] - 1) }; notes.push(`${key} lost 1 point from ${getLineageRiskLabel(risk)} pressure.`); } }); return { stats: nextStats, notes }; }

function pickVariant(seed: string, giver?: CreatureRecord, receiver?: CreatureRecord): VariantId { const receiverVariant = receiver ? getVariantDefinition(receiver.variantId) : null; const giverVariant = giver ? getVariantDefinition(giver.variantId) : null; const preferredFamily = receiverVariant?.family ?? giverVariant?.family ?? "feline"; const variants = getVariantsForFamily(preferredFamily); const commonVariant = variants.find((variant) => variant.rarity === "Common") ?? variants[0]; const rareVariants = variants.filter((variant) => variant.rarity !== "Common"); const rareRoll = deterministicRoll(`${seed}_variant`, 100); if (rareRoll >= 93 && rareVariants.length) return rareVariants[deterministicRoll(`${seed}_rare_variant`, rareVariants.length)].variantId; if (receiverVariant && receiverVariant.rarity !== "Common" && rareRoll >= 86) return receiverVariant.variantId; if (giverVariant && giverVariant.rarity !== "Common" && rareRoll >= 90) return giverVariant.variantId; return commonVariant.variantId; }

function buildInheritedAbilities(seed: string, speciesAbilities: CreatureAbility[], variantAbilities: CreatureAbility[], risk: CreatureLineageRisk, breedingTier: number, giver?: CreatureRecord, receiver?: CreatureRecord): { abilities: CreatureAbility[]; notes: string[] } { const tier = Math.max(0, Math.min(4, breedingTier)); const notes: string[] = []; const inheritedPool = [...(giver?.abilities ?? []), ...(receiver?.abilities ?? [])]; const chosen: CreatureAbility[] = []; const uniqueInheritedPool = inheritedPool.filter((ability, index, pool) => pool.findIndex((item) => item.id === ability.id) === index); if (uniqueInheritedPool.length) { const baseChance = BREEDING_ABILITY_INHERIT_CHANCE_BY_TIER[tier] ?? 16; const inheritChance = baseChance + (uniqueInheritedPool.length >= 2 ? 8 : 0); if (deterministicRoll(`${seed}_ability_inherit`, 100) < inheritChance) { const inheritedAbility = uniqueInheritedPool[deterministicRoll(`${seed}_ability_pick`, uniqueInheritedPool.length)]; chosen.push(inheritedAbility); notes.push(`${inheritedAbility.name} was inherited from a contributor. Breeding Pen Comfort Tier ${tier} helped set the inheritance odds.`); } const secondChance = BREEDING_SECOND_ABILITY_CHANCE_BY_TIER[tier] ?? 0; if (chosen.length && uniqueInheritedPool.length > 1 && deterministicRoll(`${seed}_ability_second_inherit`, 100) < secondChance) { const remaining = uniqueInheritedPool.filter((ability) => ability.id !== chosen[0].id); const inheritedAbility = remaining[deterministicRoll(`${seed}_ability_second_pick`, remaining.length)]; chosen.push(inheritedAbility); notes.push(`${inheritedAbility.name} also carried through the lineage.`); } } const mutationChance = (BREEDING_NEW_ABILITY_CHANCE_BY_TIER[tier] ?? 1) + (risk === "none" ? 0 : 1); if (deterministicRoll(`${seed}_ability_mutation`, 100) < mutationChance) { const mutationPool = [...speciesAbilities, ...variantAbilities].filter((ability) => !chosen.some((chosenAbility) => chosenAbility.id === ability.id)); if (mutationPool.length) { const mutatedAbility = mutationPool[deterministicRoll(`${seed}_ability_mutation_pick`, mutationPool.length)]; chosen.push({ ...mutatedAbility, source: "future" }); notes.push(`${mutatedAbility.name} appeared as an extremely rare new hatch mutation.`); } } if (!notes.length) notes.push(uniqueInheritedPool.length ? `No ability carried through this time. Contributor abilities are uncommon at Breeding Pen Comfort Tier ${tier}; upgrades improve the odds.` : "No contributor ability was available to inherit. New hatch ability mutations are extremely rare."); return { abilities: chosen.slice(0, 2), notes }; }

export function createInheritancePreview(save: GameSave, giverParticipant: BreedingParticipant, receiverParticipant: BreedingParticipant, seed: string): InheritancePreview { const giverCreature = giverParticipant.creatureId ? (save.creatures ?? []).find((creature) => creature.creatureId === giverParticipant.creatureId) : undefined; const receiverCreature = receiverParticipant.creatureId ? (save.creatures ?? []).find((creature) => creature.creatureId === receiverParticipant.creatureId) : undefined; const projectedVariantId = pickVariant(seed, giverCreature, receiverCreature); const projectedVariant = getVariantDefinition(projectedVariantId); const projectedSpecies = getSpeciesDefinition(projectedVariant.speciesId); const lineage = analyzeLineage(giverCreature, receiverCreature); const breedingTier = getRanchUpgrades(save).breeding_pen_comfort ?? 0; const gradeResult = inheritStatGrades(seed, giverCreature, receiverCreature); const lineageGradeResult = applyLineageRiskToGrades(seed, gradeResult.statGrades, lineage.risk); const variantAdjustedStats = STAT_KEYS.reduce((stats, key) => ({ ...stats, [key]: Math.max(1, projectedSpecies.baseStats[key] + (projectedVariant.statAdjustments[key] ?? 0)) }), {} as CreatureStats); const statResult = buildInheritedStats(seed, variantAdjustedStats, lineageGradeResult.statGrades, giverCreature, receiverCreature); const lineageStatResult = applyLineageRiskToStats(seed, statResult.stats, lineage.risk); const abilityResult = buildInheritedAbilities(seed, projectedSpecies.exclusiveAbilityPool, projectedVariant.exclusiveAbilityPool, lineage.risk, breedingTier, giverCreature, receiverCreature); const provisionalEgg = { eggId: `${seed}_egg` as EggId, parents: { giver: parentSnapshot(giverParticipant), receiver: parentSnapshot(receiverParticipant) }, variantId: projectedVariant.variantId, speciesId: projectedSpecies.speciesId }; return { projectedSpeciesId: projectedSpecies.speciesId, projectedVariantId: projectedVariant.variantId, projectedStats: lineageStatResult.stats, projectedStatGrades: lineageGradeResult.statGrades, projectedAbilities: abilityResult.abilities, statRollNotes: [...gradeResult.notes, ...lineageGradeResult.notes, ...statResult.notes, ...lineageStatResult.notes], abilityRollNotes: abilityResult.notes, lineageRisk: lineage.risk, lineageRiskLabel: lineage.label, lineageNotes: [...lineage.notes, ...lineageGradeResult.notes, ...lineageStatResult.notes], lineageTraits: lineageGradeResult.traits, suggestedName: suggestHatchlingName(provisionalEgg) }; }

export function createPregnancyRecord(save: GameSave, giverParticipant: BreedingParticipant, receiverParticipant: BreedingParticipant, seed: string): PregnancyRecord { const inheritance = createInheritancePreview(save, giverParticipant, receiverParticipant, seed); const pregnancyId = `pregnancy_${save.dayState.dayNumber}_${Date.now()}` as PregnancyId; const pregnancyDays = getRanchUpgradeEffects(save).nurseryPregnancyDays; return { pregnancyId, createdAtDayNumber: save.dayState.dayNumber, createdAt: new Date().toISOString(), daysRemaining: pregnancyDays, totalDays: pregnancyDays, status: "pregnant", giver: parentSnapshot(giverParticipant), receiver: parentSnapshot(receiverParticipant), inheritance }; }
function createEggFromPregnancy(save: GameSave, pregnancy: PregnancyRecord): EggRecord { const variant = getVariantDefinition(pregnancy.inheritance.projectedVariantId); const eggId = `egg_${save.dayState.dayNumber}_${Date.now()}_${pregnancy.pregnancyId}` as EggId; const eggDays = getRanchUpgradeEffects(save).nurseryEggDays; return { eggId, ownerSaveId: save.saveId, createdAtDayNumber: save.dayState.dayNumber, createdAt: new Date().toISOString(), daysRemaining: eggDays, totalDays: eggDays, status: "incubating", rarity: variant.rarity, speciesId: pregnancy.inheritance.projectedSpeciesId, variantId: pregnancy.inheritance.projectedVariantId, habitatId: getHabitatIdForSpecies(pregnancy.inheritance.projectedSpeciesId), parents: { giver: pregnancy.giver, receiver: pregnancy.receiver }, projectedStats: pregnancy.inheritance.projectedStats, projectedStatGrades: pregnancy.inheritance.projectedStatGrades, projectedAbilities: pregnancy.inheritance.projectedAbilities, statRollNotes: pregnancy.inheritance.statRollNotes, abilityRollNotes: pregnancy.inheritance.abilityRollNotes, lineageRisk: pregnancy.inheritance.lineageRisk, lineageRiskLabel: pregnancy.inheritance.lineageRiskLabel, lineageNotes: pregnancy.inheritance.lineageNotes, lineageTraits: pregnancy.inheritance.lineageTraits, suggestedName: pregnancy.inheritance.suggestedName }; }
export function advanceNurseryDay(save: GameSave): { save: GameSave; summaryItems: string[] } { const summaryItems: string[] = []; const deliveredEggs: EggRecord[] = []; const nextPregnancies = (save.pregnancies ?? []).map((pregnancy) => { if (pregnancy.status !== "pregnant") return pregnancy; const nextDaysRemaining = Math.max(0, pregnancy.daysRemaining - 1); if (nextDaysRemaining <= 0) { const egg = createEggFromPregnancy(save, pregnancy); deliveredEggs.push(egg); summaryItems.push(`${pregnancy.receiver.displayName} produced an egg.`); return { ...pregnancy, daysRemaining: 0, status: "delivered" as const }; } summaryItems.push(`${pregnancy.receiver.displayName}'s pregnancy timer advanced.`); return { ...pregnancy, daysRemaining: nextDaysRemaining }; }); const nextEggs = [...deliveredEggs, ...(save.eggs ?? [])].map((egg) => { if (egg.status !== "incubating") return egg; const nextDaysRemaining = Math.max(0, egg.daysRemaining - 1); if (nextDaysRemaining <= 0) return { ...egg, daysRemaining: 0, status: "ready" as const }; return { ...egg, daysRemaining: nextDaysRemaining }; }); if (nextEggs.some((egg) => egg.status === "ready")) summaryItems.push("An egg is ready to hatch in the nursery."); return { save: { ...save, pregnancies: nextPregnancies, eggs: nextEggs, eggIds: nextEggs.map((egg) => egg.eggId), flags: { ...save.flags, m5NurseryTimersAdvanced: true, m85EggGradeInheritance: true, m13NurseryContentPack: true, m16NurseryTimersBalanced: true, m17LineageRiskEnabled: true, m18BreedingAbilityUpgradeOdds: true } }, summaryItems }; }
function getNextCreatureId(save: GameSave): CreatureId { return `creature_hatched_${Date.now()}_${(save.creatures ?? []).length + 1}` as CreatureId; }
export function hatchEgg(save: GameSave, eggId: EggId, nickname?: string): { save: GameSave; creature: CreatureRecord } | null { const egg = (save.eggs ?? []).find((item) => item.eggId === eggId); if (!egg || egg.status !== "ready") return null; const variant = getVariantDefinition(egg.variantId); const species = getSpeciesDefinition(egg.speciesId); const targetHabitat = (save.habitats ?? []).find((habitat) => habitat.habitatId === egg.habitatId); if (targetHabitat && targetHabitat.creatureIds.length >= targetHabitat.capacity) return null; const creatureId = getNextCreatureId(save); const level = 1; const maxEnergy = getCreatureMaxEnergyFromStats(egg.projectedStats, variant.variantId); const maxHearts = getBaseMaxHearts(species.speciesId, variant.variantId); const parentCreatureIds = [egg.parents.giver.creatureId, egg.parents.receiver.creatureId].filter(Boolean) as CreatureId[]; const generation = Math.max(2, ...parentCreatureIds.map((id) => (save.creatures ?? []).find((creature) => creature.creatureId === id)?.generation ?? 1)) + 1; const finalName = nickname?.trim() || egg.suggestedName || suggestHatchlingName(egg); const lineageRisk = egg.lineageRisk ?? "none"; const creature: CreatureRecord = { creatureId, ownerSaveId: save.saveId, speciesId: species.speciesId, variantId: variant.variantId, habitatId: egg.habitatId, nickname: finalName, level, xp: 0, xpToNext: getCreatureXpToNext(level), stats: egg.projectedStats, statGrades: egg.projectedStatGrades ?? DEFAULT_STAT_GRADES, abilities: egg.projectedAbilities ?? [], energy: maxEnergy, maxEnergy, hearts: maxHearts, maxHearts, affection: 35, generation, shiny: false, cosmeticVariant: null, origin: "hatched", originLabel: `Hatched · ${egg.parents.giver.displayName} × ${egg.parents.receiver.displayName}`, lineage: { risk: lineageRisk, label: egg.lineageRiskLabel ?? getLineageRiskLabel(lineageRisk), parentCreatureIds, parentNames: [egg.parents.giver.displayName, egg.parents.receiver.displayName], notes: egg.lineageNotes ?? [], traits: egg.lineageTraits ?? [] }, isLocked: false, createdAt: new Date().toISOString(), notes: `Hatched from ${egg.parents.giver.displayName} and ${egg.parents.receiver.displayName}. ${egg.lineageRiskLabel ?? getLineageRiskLabel(lineageRisk)}.` }; const nextEggs = (save.eggs ?? []).map((item) => item.eggId === eggId ? { ...item, status: "hatched" as const } : item); const totalHatched = Number(save.flags.m9TotalHatched ?? 0) + 1; return { creature, save: { ...save, creatures: [creature, ...(save.creatures ?? [])], creatureIds: [creature.creatureId, ...save.creatureIds], habitats: (save.habitats ?? []).map((habitat) => habitat.habitatId === egg.habitatId ? { ...habitat, creatureIds: [creature.creatureId, ...habitat.creatureIds] } : habitat), eggs: nextEggs, eggIds: nextEggs.map((item) => item.eggId), flags: { ...save.flags, m5EggHatched: true, m85HatchedStatGrades: true, m9HatchResultImproved: true, m9TotalHatched: totalHatched, m13HatchedContentPack: true, m17HatchRevealModal: true, m17LineageRiskEnabled: true, m18BreedingAbilityUpgradeOdds: true } } }; }
export function removeEgg(save: GameSave, eggId: EggId, mode: "release" | "donate"): GameSave { const nextEggs = (save.eggs ?? []).filter((egg) => egg.eggId !== eggId); return { ...save, eggs: nextEggs, eggIds: nextEggs.map((egg) => egg.eggId), currencies: mode === "donate" ? { ...save.currencies, gold: save.currencies.gold + 75, guildPoints: save.currencies.guildPoints + 1 } : save.currencies, flags: { ...save.flags, m5EggRemoved: mode } }; }
