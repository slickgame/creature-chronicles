import type { GameSave } from "@/types/save";

export type BattleOutfitterItemId = "sparring_wraps" | "guard_charm" | "focus_manual" | "team_tactics_kit" | "field_tonic" | "revival_salve";
export type BattleOutfitterCategory = "Equipment" | "Manual" | "Consumable" | "Team Prep";
export type BattleOutfitterItem = { itemId: BattleOutfitterItemId; name: string; category: BattleOutfitterCategory; description: string; costGold: number; iconPath: string; effectLabel: string; flagKey: string; maxStock?: number };
export type BattleOutfitterResult = { save: GameSave; ok: boolean; message: string };

export const DARIAN_VOSS = { npcId: "darian_voss", name: "Darian Voss", title: "Battle Outfitter", portraitPath: "/images/npcs/town/darian_voss_portrait.png", profilePath: "/images/npcs/town/darian_voss_profile.png", intro: "Darian Voss keeps the town's combat shelves stocked with safe training gear, manuals, and battle-prep supplies." } as const;

export const BATTLE_OUTFITTER_ITEMS: BattleOutfitterItem[] = [
  { itemId: "sparring_wraps", name: "Sparring Wraps", category: "Equipment", description: "Basic combat wraps used in future battle loadouts. For now, stockpiles a permanent prep item.", costGold: 160, iconPath: "/images/ui/icons/icon_training_whistle.png", effectLabel: "+1 Sparring Wraps stock. Future equipment slot item.", flagKey: "battleItem_sparringWraps" },
  { itemId: "guard_charm", name: "Guard Charm", category: "Equipment", description: "A defensive charm for future tank and protector builds.", costGold: 190, iconPath: "/images/ui/icons/icon_lock_favorite.png", effectLabel: "+1 Guard Charm stock. Future defensive equipment.", flagKey: "battleItem_guardCharms" },
  { itemId: "focus_manual", name: "Focus Manual", category: "Manual", description: "A printed move-training reference for future skill and loadout changes.", costGold: 225, iconPath: "/images/ui/icons/icon_collection_book.png", effectLabel: "+1 Focus Manual stock. Future move tutor currency.", flagKey: "battleItem_focusManuals" },
  { itemId: "team_tactics_kit", name: "Team Tactics Kit", category: "Team Prep", description: "A planning kit for future team-role setups: striker, tank, support, and control.", costGold: 275, iconPath: "/images/ui/icons/icon_guild_points.png", effectLabel: "+1 Team Tactics Kit stock. Future formation/loadout support.", flagKey: "battleItem_tacticsKits" },
  { itemId: "field_tonic", name: "Field Tonic", category: "Consumable", description: "A safe combat-prep tonic intended for future PvE challenge attempts.", costGold: 120, iconPath: "/images/ui/icons/icon_energy.png", effectLabel: "+1 Field Tonic stock. Future battle consumable.", flagKey: "battleItem_fieldTonics", maxStock: 12 },
  { itemId: "revival_salve", name: "Revival Salve", category: "Consumable", description: "Emergency salve for future combat recovery rules.", costGold: 260, iconPath: "/images/ui/icons/icon_repair_kit.png", effectLabel: "+1 Revival Salve stock. Future revive/recovery consumable.", flagKey: "battleItem_revivalSalves", maxStock: 6 },
];

function getFlagNumber(value: boolean | number | string | undefined): number { const parsed = typeof value === "number" ? value : Number(value ?? 0); return Number.isFinite(parsed) ? Math.max(0, Math.floor(parsed)) : 0; }
export function getBattleOutfitterStock(save: GameSave, item: BattleOutfitterItem): number { return getFlagNumber(save.flags[item.flagKey]); }
export function getBattleOutfitterSummary(save: GameSave): { totalStock: number; equipmentStock: number; manualStock: number; consumableStock: number } { return BATTLE_OUTFITTER_ITEMS.reduce((summary, item) => { const stock = getBattleOutfitterStock(save, item); return { totalStock: summary.totalStock + stock, equipmentStock: summary.equipmentStock + (item.category === "Equipment" ? stock : 0), manualStock: summary.manualStock + (item.category === "Manual" ? stock : 0), consumableStock: summary.consumableStock + (item.category === "Consumable" ? stock : 0) }; }, { totalStock: 0, equipmentStock: 0, manualStock: 0, consumableStock: 0 }); }
export function purchaseBattleOutfitterItem(save: GameSave, itemId: string): BattleOutfitterResult { const item = BATTLE_OUTFITTER_ITEMS.find((entry) => entry.itemId === itemId); if (!item) return { save, ok: false, message: "Darian cannot find that item." }; const currentStock = getBattleOutfitterStock(save, item); if (item.maxStock && currentStock >= item.maxStock) return { save, ok: false, message: `${item.name} stock is already full (${currentStock}/${item.maxStock}).` }; if (save.currencies.gold < item.costGold) return { save, ok: false, message: `Need ${item.costGold} Gold for ${item.name}.` }; return { save: { ...save, updatedAt: new Date().toISOString(), currencies: { ...save.currencies, gold: save.currencies.gold - item.costGold }, flags: { ...save.flags, m51BattleOutfitter: true, [item.flagKey]: currentStock + 1 } }, ok: true, message: `${item.name} purchased. ${item.effectLabel}` }; }
