"use client";

import { useMemo, useState } from "react";
import { assignBattleOutfitterEquipment, BATTLE_OUTFITTER_ITEMS, DARIAN_VOSS, getBattleLoadout, getBattleOutfitterStock, getBattleOutfitterSummary, purchaseBattleOutfitterItem, removeBattleOutfitterEquipment, useBattleOutfitterManual } from "@/data/battleOutfitter";
import { getVariantDefinition } from "@/data/creatures";
import { formatGold } from "@/lib/formatters";
import { useGameContext } from "@/state/GameProvider";
import type { BattleLoadoutSlot, BattleOutfitterCategory, BattleOutfitterItemId } from "@/data/battleOutfitter";
import type { CreatureRecord } from "@/types/creature";
import type { CreatureId } from "@/types/ids";
import styles from "@/features/market/MarketScreen.module.css";

const CATEGORIES: Array<BattleOutfitterCategory | "All"> = ["All", "Equipment", "Manual", "Consumable", "Team Prep"];
const ICONS = { interior: "/images/buildings/town/battle_outfitter_interior.png", darian: DARIAN_VOSS.portraitPath, fallback: "/images/buildings/town/battle_outfitter.png", gold: "/images/ui/currency/icon_currency_gold.png", manual: "/images/ui/icons/icon_collection_book.png" } as const;
type OutfitterMode = "interior" | "shop" | "talk" | "stock" | "loadouts";
function panelStyle() { return { position: "relative", zIndex: 3, padding: 18, border: "1px solid rgba(245,201,128,.55)", borderRadius: 4, background: "rgba(21, 10, 5, .74)", color: "#fff0c9", boxShadow: "0 14px 34px rgba(0,0,0,.42)" } as const; }
function hotspotStyle() { return { display: "grid", gap: 4, justifyItems: "center", minWidth: 220, padding: "18px 20px", border: "2px solid rgba(245,201,128,.72)", borderRadius: 4, background: "rgba(0,0,0,.44)", color: "#fff7dd", boxShadow: "0 14px 28px rgba(0,0,0,.35)", cursor: "pointer" } as const; }
function getCreatureImage(creature: CreatureRecord): string { return getVariantDefinition(creature.variantId).portraitPath; }
function getItemName(itemId: string | null): string { return BATTLE_OUTFITTER_ITEMS.find((item) => item.itemId === itemId)?.name ?? "Empty"; }

export function BattleOutfitterScreen() {
  const { currentSave, goToMainMenu, goToTown, saveCurrentGame } = useGameContext();
  const [mode, setMode] = useState<OutfitterMode>("interior");
  const [category, setCategory] = useState<BattleOutfitterCategory | "All">("All");
  const [selectedCreatureId, setSelectedCreatureId] = useState<CreatureId | null>(null);
  const [message, setMessage] = useState("Darian keeps the combat shelves ready: equipment stock, move manuals, battle consumables, and team-prep kits.");
  const visibleItems = useMemo(() => BATTLE_OUTFITTER_ITEMS.filter((item) => category === "All" || item.category === category), [category]);
  const equipmentItems = useMemo(() => BATTLE_OUTFITTER_ITEMS.filter((item) => item.category === "Equipment"), []);
  if (!currentSave) return <main className={styles.emptyScreen}><section className={styles.emptyPanel}><h1>No active save</h1><p>Load or create a save before visiting the Battle Outfitter.</p><button type="button" onClick={goToMainMenu}>Return to Main Menu</button></section></main>;
  const creatures = currentSave.creatures ?? [];
  const selectedCreature = creatures.find((creature) => creature.creatureId === selectedCreatureId) ?? creatures[0] ?? null;
  const summary = getBattleOutfitterSummary(currentSave);
  function applyResult(result: ReturnType<typeof purchaseBattleOutfitterItem>, nextMode: OutfitterMode) { if (result.ok) saveCurrentGame(result.save); setMessage(result.message); setMode(nextMode); }
  function buyItem(itemId: string) { applyResult(purchaseBattleOutfitterItem(currentSave, itemId), "shop"); }
  function assignItem(creatureId: CreatureId, itemId: BattleOutfitterItemId) { applyResult(assignBattleOutfitterEquipment(currentSave, creatureId, itemId), "loadouts"); }
  function removeItem(creatureId: CreatureId, slot: BattleLoadoutSlot) { applyResult(removeBattleOutfitterEquipment(currentSave, creatureId, slot), "loadouts"); }
  function useManual(creatureId: CreatureId) { applyResult(useBattleOutfitterManual(currentSave, creatureId), "loadouts"); }
  return <main className={styles.screen}><section className={styles.frame}><div className={styles.backgroundArt} aria-hidden="true" /><div className={styles.shade} aria-hidden="true" /><header className={styles.header}><div><p className={styles.kicker}>M52 Battle Loadouts</p><h1>Battle Outfitter</h1><p>{DARIAN_VOSS.name}, {DARIAN_VOSS.title}, prepares future combat gear, manuals, consumables, and team tools.</p><p className={styles.message}>{message}</p></div><div className={styles.headerActions}><div className={styles.statBox}><span>Gold</span><strong>{formatGold(currentSave.currencies.gold)}</strong></div><div className={styles.statBox}><span>Stock</span><strong>{summary.totalStock}</strong></div><div className={styles.statBox}><span>Assigned</span><strong>{summary.assignedEquipment}</strong></div><div className={styles.statBox}><span>Manual Ranks</span><strong>{summary.manualRanks}</strong></div><button type="button" className={styles.backButton} onClick={goToTown}>Back to Town</button><button type="button" className={styles.backButton} onClick={goToMainMenu}>Main Menu</button></div></header>{mode === "interior" ? <section aria-label="Battle Outfitter interior" style={{ position: "relative", zIndex: 2, minHeight: "calc(100vh - 230px)", padding: "28px 34px 34px", overflow: "hidden" }}><div aria-hidden="true" style={{ position: "absolute", inset: 0, background: `linear-gradient(180deg, rgba(0,0,0,.10), rgba(0,0,0,.62)), url(${ICONS.interior}) center/cover`, opacity: .44 }} /><div style={{ position: "relative", zIndex: 3, display: "grid", gridTemplateColumns: "minmax(260px, 360px) minmax(0, 1fr)", gap: 26, alignItems: "end", minHeight: "58vh" }}><aside style={panelStyle()}><div style={{ display: "grid", gridTemplateColumns: "76px minmax(0,1fr)", gap: 12, alignItems: "center" }}><img src={ICONS.darian} alt="" onError={(event) => { event.currentTarget.src = ICONS.fallback; }} style={{ width: 76, height: 76, objectFit: "cover", borderRadius: 999, border: "1px solid rgba(245,201,128,.55)" }} /><div><p className={styles.kicker}>Outfitter</p><h2 style={{ margin: 0 }}>Darian Voss</h2><p style={{ margin: "6px 0 0" }}>{summary.totalStock} stocked • {summary.assignedEquipment} assigned</p></div></div><p style={{ marginTop: 16, lineHeight: 1.55 }}>&quot;Training makes a creature better. Loadouts make a team ready. Start assigning gear now so the future battle update has hooks already in the save.&quot;</p><div style={{ display: "grid", gap: 8 }}><button type="button" className={styles.buyButton} onClick={() => setMode("shop")}>Open Shelves</button><button type="button" className={styles.buyButton} onClick={() => setMode("loadouts")}>Creature Loadouts</button><button type="button" className={styles.buyButton} onClick={() => setMode("stock")}>Stock Ledger</button><button type="button" className={styles.buyButton} onClick={() => setMode("talk")}>Talk to Darian</button></div></aside><div style={{ position: "relative", minHeight: 430 }}><button type="button" style={{ ...hotspotStyle(), position: "absolute", left: "10%", top: "38%" }} onClick={() => setMode("shop")}><img src={ICONS.fallback} alt="" style={{ width: 58, height: 58, objectFit: "cover", borderRadius: 999 }} /><strong>Combat Shelves</strong><span style={{ color: "#7fdbff", fontWeight: 900 }}>Gear • Manuals • Consumables</span></button><button type="button" style={{ ...hotspotStyle(), position: "absolute", right: "8%", top: "30%" }} onClick={() => setMode("talk")}><img src={ICONS.darian} alt="" onError={(event) => { event.currentTarget.src = ICONS.fallback; }} style={{ width: 66, height: 66, objectFit: "cover", borderRadius: 999 }} /><strong>Darian Voss</strong><span style={{ color: "#7fdbff", fontWeight: 900 }}>Combat prep advice</span></button><button type="button" style={{ ...hotspotStyle(), position: "absolute", left: "39%", bottom: "8%" }} onClick={() => setMode("loadouts")}><img src={ICONS.manual} alt="" style={{ width: 58, height: 58, objectFit: "cover", borderRadius: 999 }} /><strong>Loadout Bench</strong><span style={{ color: "#7fdbff", fontWeight: 900 }}>Assign equipment</span></button></div></div></section> : null}{mode === "talk" ? <section style={{ position: "relative", zIndex: 3, padding: 24, display: "grid", gridTemplateColumns: "320px minmax(0, 1fr)", gap: 18 }}><aside style={panelStyle()}><img src={ICONS.darian} alt="" onError={(event) => { event.currentTarget.src = ICONS.fallback; }} style={{ width: "100%", maxHeight: 280, objectFit: "cover", borderRadius: 8, border: "1px solid rgba(245,201,128,.45)" }} /><h2>Darian Voss</h2><p>{DARIAN_VOSS.title}</p></aside><section style={panelStyle()}><p className={styles.kicker}>Combat Prep</p><h2>Loadouts Before Battles</h2><p style={{ fontSize: "1.05rem", lineHeight: 1.65 }}>&quot;The Coliseum can wait. Prepared teams should not. Wraps and charms are simple equipment hooks. Manuals are a quiet way to mark who is ready for move training later.&quot;</p><p style={{ lineHeight: 1.6 }}>Equipment assigned here leaves stock and goes onto a creature. Removing it returns the item to stock.</p><button type="button" className={styles.buyButton} onClick={() => setMode("loadouts")}>Open Loadouts</button><button type="button" className={styles.backButton} onClick={() => setMode("interior")}>Back to Outfitter</button></section></section> : null}{mode === "stock" ? <StockPanel save={currentSave} summary={summary} onBack={() => setMode("interior")} /> : null}{mode === "shop" ? <ShopPanel save={currentSave} category={category} setCategory={setCategory} visibleItems={visibleItems} onBuy={buyItem} onBack={() => setMode("interior")} /> : null}{mode === "loadouts" ? <LoadoutPanel save={currentSave} creatures={creatures} selectedCreature={selectedCreature} setSelectedCreatureId={setSelectedCreatureId} equipmentItems={equipmentItems} onAssign={assignItem} onRemove={removeItem} onUseManual={useManual} onBack={() => setMode("interior")} /> : null}</section></main>;
}

function StockPanel({ save, summary, onBack }: { save: NonNullable<ReturnType<typeof useGameContext>["currentSave"]>; summary: ReturnType<typeof getBattleOutfitterSummary>; onBack: () => void }) { return <section className={styles.grid} style={{ position: "relative", zIndex: 3, padding: "14px 18px 24px" }}><aside className={styles.panel}><h2>Stock Ledger</h2><p>These are future combat resources already stored on the save.</p><div className={styles.sideList}><div className={styles.infoCard}><span>Total Stock</span><strong>{summary.totalStock}</strong></div><div className={styles.infoCard}><span>Assigned Gear</span><strong>{summary.assignedEquipment}</strong></div><div className={styles.infoCard}><span>Manual Ranks</span><strong>{summary.manualRanks}</strong></div><button type="button" className={styles.backButton} onClick={onBack}>Back to Outfitter</button></div></aside><section className={styles.panel}><h2>Owned Stock</h2><div className={styles.listings}>{BATTLE_OUTFITTER_ITEMS.map((item) => <article key={item.itemId} className={styles.listing}><div className={styles.listingArt}><img src={item.iconPath} alt="" onError={(event) => { event.currentTarget.src = ICONS.fallback; }} /></div><div className={styles.listingBody}><span className={styles.listingMeta}>{item.category}</span><h3 className={styles.listingName}>{item.name}</h3><p className={styles.listingDesc}>{item.effectLabel}</p><p className={styles.gradePreview}>Owned: {getBattleOutfitterStock(save, item)}{item.maxStock ? `/${item.maxStock}` : ""}</p></div></article>)}</div></section></section>; }
function ShopPanel({ save, category, setCategory, visibleItems, onBuy, onBack }: { save: NonNullable<ReturnType<typeof useGameContext>["currentSave"]>; category: BattleOutfitterCategory | "All"; setCategory: (category: BattleOutfitterCategory | "All") => void; visibleItems: typeof BATTLE_OUTFITTER_ITEMS; onBuy: (itemId: string) => void; onBack: () => void }) { return <section className={styles.grid} style={{ position: "relative", zIndex: 3, padding: "14px 18px 24px" }}><aside className={styles.panel}><h2>Combat Shelves</h2><p>Buy future battle-prep stock with Gold.</p><div className={styles.sideList}>{CATEGORIES.map((item) => <button key={item} type="button" className={styles.infoCard} onClick={() => setCategory(item)}><span>Category</span><strong>{item === category ? `› ${item}` : item}</strong></button>)}<button type="button" className={styles.backButton} onClick={onBack}>Back to Outfitter</button></div></aside><section className={styles.panel}><h2>{category} Items</h2><div className={styles.listings}>{visibleItems.map((item) => { const stock = getBattleOutfitterStock(save, item); const full = Boolean(item.maxStock && stock >= item.maxStock); const canAfford = save.currencies.gold >= item.costGold; return <article key={item.itemId} className={`${styles.listing} ${full ? styles.sold : ""}`}><div className={styles.listingArt}><img src={item.iconPath} alt="" onError={(event) => { event.currentTarget.src = ICONS.fallback; }} /></div><div className={styles.listingBody}><span className={styles.listingMeta}>{item.category}</span><h3 className={styles.listingName}>{item.name}</h3><p className={styles.listingDesc}>{item.description}</p><p className={styles.gradePreview}>{item.effectLabel}</p><div className={styles.price}><img src={ICONS.gold} alt="" /><div><span>Cost</span><strong>{formatGold(item.costGold)}</strong><em>Owned: {stock}{item.maxStock ? `/${item.maxStock}` : ""}</em>{full ? <em>Stock full.</em> : !canAfford ? <em>Not enough Gold.</em> : null}</div></div><div className={styles.actions}><button type="button" className={styles.buyButton} disabled={full || !canAfford} onClick={() => onBuy(item.itemId)}>{full ? "Full" : "Buy"}</button></div></div></article>; })}</div></section></section>; }
function LoadoutPanel({ save, creatures, selectedCreature, setSelectedCreatureId, equipmentItems, onAssign, onRemove, onUseManual, onBack }: { save: NonNullable<ReturnType<typeof useGameContext>["currentSave"]>; creatures: CreatureRecord[]; selectedCreature: CreatureRecord | null; setSelectedCreatureId: (id: CreatureId) => void; equipmentItems: typeof BATTLE_OUTFITTER_ITEMS; onAssign: (creatureId: CreatureId, itemId: BattleOutfitterItemId) => void; onRemove: (creatureId: CreatureId, slot: BattleLoadoutSlot) => void; onUseManual: (creatureId: CreatureId) => void; onBack: () => void }) { const loadout = selectedCreature ? getBattleLoadout(save, selectedCreature.creatureId) : null; const manualItem = BATTLE_OUTFITTER_ITEMS.find((item) => item.itemId === "focus_manual"); const manualStock = manualItem ? getBattleOutfitterStock(save, manualItem) : 0; return <section className={styles.grid} style={{ position: "relative", zIndex: 3, padding: "14px 18px 24px" }}><aside className={styles.panel}><h2>Creature Loadouts</h2><p>Assign equipment and manuals to make future combat hooks creature-specific.</p><div className={styles.sideList}>{creatures.map((creature) => { const creatureLoadout = getBattleLoadout(save, creature.creatureId); return <button key={creature.creatureId} type="button" className={styles.infoCard} onClick={() => setSelectedCreatureId(creature.creatureId)}><img src={getCreatureImage(creature)} alt="" onError={(event) => { event.currentTarget.src = ICONS.fallback; }} /><span>{getVariantDefinition(creature.variantId).name}</span><strong>{creature.nickname}</strong><em>Readiness {creatureLoadout.readinessScore} • {creatureLoadout.labels.join(" • ") || "No loadout"}</em></button>; })}<button type="button" className={styles.backButton} onClick={onBack}>Back to Outfitter</button></div></aside><section className={styles.panel}>{selectedCreature && loadout ? <><h2>{selectedCreature.nickname}'s Battle Prep</h2><article className={styles.listing}><div className={styles.listingArt}><img src={getCreatureImage(selectedCreature)} alt="" onError={(event) => { event.currentTarget.src = ICONS.fallback; }} /></div><div className={styles.listingBody}><span className={styles.listingMeta}>Readiness {loadout.readinessScore}</span><h3 className={styles.listingName}>{selectedCreature.nickname}</h3><p className={styles.listingDesc}>Offense: {getItemName(loadout.offenseItemId)} • Defense: {getItemName(loadout.defenseItemId)} • Manual Rank {loadout.manualRank}/3</p><div className={styles.actions}><button type="button" className={styles.buyButton} disabled={!loadout.offenseItemId} onClick={() => onRemove(selectedCreature.creatureId, "offense")}>Remove Offense</button><button type="button" className={styles.buyButton} disabled={!loadout.defenseItemId} onClick={() => onRemove(selectedCreature.creatureId, "defense")}>Remove Defense</button><button type="button" className={styles.buyButton} disabled={manualStock <= 0 || loadout.manualRank >= 3} onClick={() => onUseManual(selectedCreature.creatureId)}>Use Focus Manual</button></div></div></article><h3>Assign Equipment</h3><div className={styles.listings}>{equipmentItems.map((item) => { const stock = getBattleOutfitterStock(save, item); return <article key={item.itemId} className={`${styles.listing} ${stock <= 0 ? styles.sold : ""}`}><div className={styles.listingArt}><img src={item.iconPath} alt="" onError={(event) => { event.currentTarget.src = ICONS.fallback; }} /></div><div className={styles.listingBody}><span className={styles.listingMeta}>{item.loadoutSlot} Slot</span><h3 className={styles.listingName}>{item.name}</h3><p className={styles.listingDesc}>{item.description}</p><p className={styles.gradePreview}>Stock: {stock}</p><div className={styles.actions}><button type="button" className={styles.buyButton} disabled={stock <= 0} onClick={() => onAssign(selectedCreature.creatureId, item.itemId)}>Assign</button></div></div></article>; })}</div></> : <div className={styles.infoCard}><strong>No creature selected.</strong></div>}</section></section>; }
