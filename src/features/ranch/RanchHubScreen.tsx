"use client";

import { type CSSProperties, useMemo, useState } from "react";
import { getVariantDefinition } from "@/data/creatures";
import { RANCH_JOB_DEFINITIONS } from "@/data/ranchJobs";
import { RANCH_UPGRADE_ASSETS } from "@/data/ranchUpgrades";
import { getStarterGoalProgress, getStarterGoals } from "@/data/starterGoals";
import { TAX_COLLECTOR } from "@/data/taxCollector";
import { formatEnergy, formatGameDate, formatGold, formatGuildPoints } from "@/lib/formatters";
import { useGameContext, type DayAdvanceResult } from "@/state/GameProvider";
import type { CreatureRecord } from "@/types/creature";
import type { CreatureId } from "@/types/ids";
import type { RanchJobId } from "@/types/ranchJobs";
import styles from "./RanchHubScreen.module.css";
import reportStyles from "./RanchHubReport.module.css";

type ModalMode = "none" | "sleep-confirm" | "day-summary" | "requests" | "starter-goals" | "coming-soon" | "ranch-info" | "nav-menu";
type InfoModalMode = "sleep" | "day-summary" | null;
type ReportPage = "overview" | "security" | "upkeep" | "chores" | "tax" | "log";
type BuildingId = "house" | "office" | "jobs" | "feline" | "canine" | "bovine" | "lapine" | "equine" | "breeding" | "nursery" | "town" | "guild";
type Building = { id: BuildingId; title: string; milestone: "Available" | "M3" | "M4" | "M5" | "M6" | "M7" | "M11" | "M13" | "M14"; description: string; actionLabel: string; imageSrc: string; x: number; y: number; width: number };

const HUD_ICONS = { crest: "/images/ui/icons/icon_paw_crest.png", energy: "/images/ui/icons/icon_energy_lightning.png", calendar: "/images/ui/icons/icon_calendar.png", home: "/images/ui/icons/icon_home.png", sleep: "/images/ui/icons/icon_sleep_moon.png", requests: "/images/ui/home/icon_home_requests.png", gold: "/images/ui/currency/icon_currency_gold.png", collection: "/images/ui/icons/icon_collection_book.png", ledger: "/images/ui/icons/icon_ranch_ledger.png", dev: "/images/ui/icons/icon_ranch_upgrade.png", goals: "/images/ui/icons/icon_collection_book.png", jobs: "/images/ui/icons/icon_ranch_upgrade.png", menu: "/images/ui/icons/icon_collection_book.png" } as const;
const BUILDINGS: Building[] = [
  { id: "house", title: "Ranch House", milestone: "Available", description: "Rest for the night and advance to the next day.", actionLabel: "Sleep", imageSrc: "/images/buildings/ranch/ranch_house.png", x: 48, y: 59, width: 11 },
  { id: "office", title: "Ranch Office", milestone: "M11", description: "Open the Ranch Office ledger to expand habitats, nursery capacity, breeding comfort, and sleep recovery.", actionLabel: "Open Ledger", imageSrc: RANCH_UPGRADE_ASSETS.officeBuilding, x: 36, y: 59, width: 10 },
  { id: "jobs", title: "Ranch Chores", milestone: "M14", description: "Assign creatures to daily ranch chores such as security, comfort, production, garden tending, and hauling.", actionLabel: "Open Chores", imageSrc: "/images/buildings/ranch/guild_board.png", x: 60, y: 58, width: 8 },
  { id: "feline", title: "Feline Habitat", milestone: "M3", description: "Home for Feline, Sphinx, and Tiger creatures.", actionLabel: "Open Habitat", imageSrc: "/images/buildings/ranch/feline_habitat.png", x: 18, y: 62, width: 10 },
  { id: "canine", title: "Canine Habitat", milestone: "M3", description: "Home for Canine, Hellhound, and Direwolf creatures.", actionLabel: "Open Habitat", imageSrc: "/images/buildings/ranch/canine_habitat.png", x: 75, y: 58, width: 10 },
  { id: "bovine", title: "Bovine Habitat", milestone: "M13", description: "Home for Cow, Minotaur, Moon Yak, and future ranch-production creatures.", actionLabel: "Open Habitat", imageSrc: "/images/buildings/ranch/bovine_habitat.png", x: 17, y: 78, width: 8 },
  { id: "lapine", title: "Lapine Habitat", milestone: "M13", description: "Home for Bunny, Antlerhare, Dream Lop, and future garden or nursery creatures.", actionLabel: "Open Habitat", imageSrc: "/images/buildings/ranch/lapine_habitat.png", x: 71, y: 78, width: 7 },
  { id: "equine", title: "Equine Habitat", milestone: "M13", description: "Home for Horse, Unicorn, Nightmare, and future travel or field creatures.", actionLabel: "Open Habitat", imageSrc: "/images/buildings/ranch/equine_habitat.png", x: 84, y: 66, width: 8 },
  { id: "breeding", title: "Breeding Pen", milestone: "M4", description: "Pair selection, breeding previews, streaks, costs, and result placeholders.", actionLabel: "Open Breeding", imageSrc: "/images/buildings/ranch/breeding_pen.png", x: 49, y: 76, width: 10 },
  { id: "nursery", title: "Egg Nursery", milestone: "M5", description: "Track pregnancies, incubating eggs, ready eggs, and hatch results.", actionLabel: "Open Nursery", imageSrc: "/images/buildings/ranch/egg_nursery.png", x: 32, y: 78, width: 9 },
  { id: "town", title: "Town Road", milestone: "M6", description: "Leave the ranch and travel to town for the market, guild board, and future town services.", actionLabel: "Travel to Town", imageSrc: "/images/buildings/ranch/town_road.png", x: 63, y: 87, width: 8 },
  { id: "guild", title: "Guild Board", milestone: "M7", description: "Legacy ranch board. The full Guild Hall now lives in town.", actionLabel: "Town Guild", imageSrc: "/images/buildings/ranch/guild_board.png", x: 82, y: 82, width: 8 },
];

function getBuildingStyle(building: Building): CSSProperties { return { left: `${building.x}%`, top: `${building.y}%`, width: `${building.width}%` }; }
function getCreatureProfilePath(creature: CreatureRecord): string { return getVariantDefinition(creature.variantId).profilePath; }
function isAvailableBuilding(id: BuildingId): boolean { return id === "house" || id === "office" || id === "jobs" || id === "feline" || id === "canine" || id === "bovine" || id === "lapine" || id === "equine" || id === "breeding" || id === "nursery" || id === "town"; }
function getFlagNumber(value: boolean | number | string | undefined): number { const parsed = typeof value === "number" ? value : Number(value ?? 0); return Number.isFinite(parsed) ? Math.max(0, Math.floor(parsed)) : 0; }
function getFeedFromMessage(message: string): number { const match = message.match(/\+(\d+)\s*Feed/i); return match ? Number(match[1]) : 0; }
function getMaterialsFromMessage(message: string): number { const match = message.match(/\+(\d+)\s*Materials/i); return match ? Number(match[1]) : 0; }

export function RanchHubScreen() {
  const { advanceDay, currentSave, goToBreeding, goToCollection, goToDevTools, goToHabitat, goToMainMenu, goToNursery, goToRanchJobs, goToRanchOffice, goToTown, version } = useGameContext();
  const [modalMode, setModalMode] = useState<ModalMode>("none");
  const [infoModalMode, setInfoModalMode] = useState<InfoModalMode>(null);
  const [daySummary, setDaySummary] = useState<DayAdvanceResult | null>(null);
  const [reportPage, setReportPage] = useState<ReportPage>("overview");
  const [message, setMessage] = useState("Welcome back to the ranch.");
  const [selectedBuildingId, setSelectedBuildingId] = useState<BuildingId>("house");
  const selectedBuilding = useMemo(() => BUILDINGS.find((building) => building.id === selectedBuildingId) ?? BUILDINGS[0], [selectedBuildingId]);
  const dateLabel = useMemo(() => currentSave ? formatGameDate(currentSave.dayState.weekday, currentSave.dayState.month, currentSave.dayState.dayOfMonth) : "Mon 1/1", [currentSave]);
  const starterGoals = useMemo(() => currentSave ? getStarterGoals(currentSave) : [], [currentSave]);
  const starterProgress = useMemo(() => currentSave ? getStarterGoalProgress(currentSave) : null, [currentSave]);

  if (!currentSave) return <main className={styles.emptyScreen}><section className={styles.emptyPanel}><h1>No active save</h1><p>Load or create a save before entering the ranch.</p><button type="button" onClick={goToMainMenu}>Return to Main Menu</button></section></main>;

  function closeModal() { setInfoModalMode(null); setModalMode("none"); }
  function handleBuildingClick(building: Building) {
    setInfoModalMode(null);
    setSelectedBuildingId(building.id);
    if (building.id === "house") { setMessage("Ranch House selected. Rest here to advance the day."); setModalMode("sleep-confirm"); return; }
    if (building.id === "office") { goToRanchOffice(); return; }
    if (building.id === "jobs") { goToRanchJobs(); return; }
    if (building.id === "feline") { goToHabitat("feline"); return; }
    if (building.id === "canine") { goToHabitat("canine"); return; }
    if (building.id === "bovine") { goToHabitat("bovine"); return; }
    if (building.id === "lapine") { goToHabitat("lapine"); return; }
    if (building.id === "equine") { goToHabitat("equine"); return; }
    if (building.id === "breeding") { goToBreeding(); return; }
    if (building.id === "nursery") { goToNursery(); return; }
    if (building.id === "town") { goToTown(); return; }
    setMessage(`${building.title} is now handled through the Town Guild Hall.`);
    setModalMode("coming-soon");
  }
  function handleSleep() { const result = advanceDay(); if (!result) return; setDaySummary(result); setReportPage("overview"); setInfoModalMode(null); setModalMode("day-summary"); }
  function getCreatureById(creatureId: CreatureId | null | undefined): CreatureRecord | null { if (!creatureId) return null; return (currentSave?.creatures ?? []).find((creature) => creature.creatureId === creatureId) ?? null; }
  function getAssignmentIds(jobId: RanchJobId): CreatureId[] { const ids = currentSave?.ranchJobs?.assignments?.[jobId] ?? []; return Array.isArray(ids) ? ids : ids ? [ids] : []; }

  const habitatCounts = (currentSave.habitats ?? []).map((habitat) => `${habitat.name.replace(" Habitat", "")} ${habitat.creatureIds.length}/${habitat.capacity}`).join(" • ");
  const nonChoreSummaryItems = (daySummary?.summaryItems ?? []).filter((item) => !(daySummary?.ranchJobResults ?? []).some((result) => result.message === item));
  const completedChores = daySummary?.ranchJobResults.length ?? 0;
  const feedProduced = getFlagNumber(currentSave.flags.ranchFeedProducedToday);
  const feedRequired = getFlagNumber(currentSave.flags.ranchFeedRequiredToday);
  const feedConsumed = getFlagNumber(currentSave.flags.ranchFeedConsumedToday);
  const feedStock = getFlagNumber(currentSave.flags.ranchFeedStock);
  const materialsStock = getFlagNumber(currentSave.flags.ranchMaterialsStock);
  const materialsProduced = getFlagNumber(currentSave.flags.ranchMaterialsProducedToday);
  const materialsSummary = String(currentSave.flags.ranchMaterialsSummaryToday ?? "No new ranch materials were hauled today.");
  const ranchDamage = getFlagNumber(currentSave.flags.ranchDamage);
  const ranchDamageAdded = getFlagNumber(currentSave.flags.ranchDamageAddedToday);
  const ranchDamageRepaired = getFlagNumber(currentSave.flags.ranchDamageRepairedToday);
  const ranchCondition = String(currentSave.flags.ranchConditionToday ?? "Good");
  const ranchConditionEnergyPenalty = getFlagNumber(currentSave.flags.ranchConditionEnergyPenaltyToday);
  const ranchConditionAffectionPenalty = getFlagNumber(currentSave.flags.ranchConditionAffectionPenaltyToday);
  const ranchConditionPenaltySummary = String(currentSave.flags.ranchConditionPenaltySummaryToday ?? "Ranch condition caused no recovery penalty.");
  const ranchPenaltyLabel = ranchConditionEnergyPenalty > 0 || ranchConditionAffectionPenalty > 0 ? `-${ranchConditionEnergyPenalty}% recovery${ranchConditionAffectionPenalty > 0 ? `, -${ranchConditionAffectionPenalty} affection` : ""}` : "No penalty";
  const ranchUpkeepSummary = String(currentSave.flags.ranchUpkeepSummaryToday ?? "Ranch condition will be checked after sleep.");
  const feedingSummary = String(currentSave.flags.ranchFeedingSummary ?? "Ranch provisions will be checked after sleep.");
  const comfortBonus = getFlagNumber(currentSave.flags.ranchBreedingComfortBonusToday);
  const securityScore = getFlagNumber(currentSave.flags.ranchSecurityScoreToday);
  const upkeepScore = getFlagNumber(currentSave.flags.ranchUpkeepScoreToday);
  const securityDangerChance = getFlagNumber(currentSave.flags.ranchSecurityDangerChanceToday);
  const securityEventType = String(currentSave.flags.ranchSecurityEventTypeToday ?? "none");
  const securityEventSummary = String(currentSave.flags.ranchSecurityEventSummaryToday ?? "Security report will appear after sleep.");
  const securitySuccess = currentSave.flags.ranchSecuritySuccessToday === true;
  const taxDue = getFlagNumber(currentSave.flags.taxCurrentMonthDue);
  const taxDaysUntilDue = getFlagNumber(currentSave.flags.taxDaysUntilDue);
  const taxStatus = String(currentSave.flags.taxStatus ?? "pending");
  const taxCollectorMessage = String(currentSave.flags.taxCollectorMessage ?? currentSave.flags.taxProjectionSummary ?? `This month's posted tax is ${taxDue} Gold.`);
  const taxReportTriggered = Boolean(daySummary?.summaryItems.some((item) => /tax|vesper|payment|collector/i.test(item)));
  const showTaxReportPage = taxStatus === "warning" || taxStatus === "paid" || taxStatus === "defaulted" || currentSave.dayState.dayOfMonth >= 25 || taxReportTriggered;
  const reportPages = useMemo<Array<{ id: ReportPage; label: string }>>(() => {
    const pages: Array<{ id: ReportPage; label: string }> = [{ id: "overview", label: "Overview" }, { id: "security", label: "Security" }, { id: "upkeep", label: "Upkeep" }, { id: "chores", label: "Chores" }];
    if (showTaxReportPage) pages.push({ id: "tax", label: "Tax" });
    pages.push({ id: "log", label: "Log" });
    return pages;
  }, [showTaxReportPage]);
  const activeReportPage = reportPages.some((page) => page.id === reportPage) ? reportPage : reportPages[0].id;
  const reportIndex = Math.max(0, reportPages.findIndex((page) => page.id === activeReportPage));
  function shiftReportPage(direction: -1 | 1) { const nextIndex = Math.max(0, Math.min(reportPages.length - 1, reportIndex + direction)); setReportPage(reportPages[nextIndex].id); }
  const groupedResults = RANCH_JOB_DEFINITIONS.map((job) => { const results = (daySummary?.ranchJobResults ?? []).filter((result) => result.jobId === job.jobId); const assignedCreatures = getAssignmentIds(job.jobId).map(getCreatureById).filter(Boolean) as CreatureRecord[]; const displayCreature = results[0] ? getCreatureById(results[0].creatureId) : assignedCreatures[0] ?? null; const totalFeed = results.reduce((total, result) => total + getFeedFromMessage(result.message), 0); const totalMaterials = results.reduce((total, result) => total + getMaterialsFromMessage(result.message), 0); return { job, results, assignedCreatures, displayCreature, totalFeed, totalMaterials }; });

  return <main className={styles.screen}><section className={styles.ranchFrame}><div className={styles.backgroundArt} aria-hidden="true" /><div className={styles.mapShade} aria-hidden="true" /><header className={styles.hud}><div className={styles.hudIdentity}><img src={HUD_ICONS.crest} alt="" /><div><span>{currentSave.player.ranchName}</span><strong>{currentSave.player.name}</strong></div></div><div className={styles.hudStats} aria-label="Player resources"><div><img src={HUD_ICONS.calendar} alt="" /><span>Date</span><strong>{dateLabel}</strong></div><div><img src={HUD_ICONS.energy} alt="" /><span>Energy</span><strong>{formatEnergy(currentSave.currencies.energy, currentSave.currencies.maxEnergy)}</strong></div><div className={styles.goldStat}><img src={HUD_ICONS.gold} alt="" /><span>Gold</span><strong>{formatGold(currentSave.currencies.gold)}</strong></div><div><img src={HUD_ICONS.ledger} alt="" /><span>Guild Points</span><strong>{formatGuildPoints(currentSave.currencies.guildPoints)}</strong></div></div><nav className={styles.hudActions} aria-label="Ranch navigation"><button type="button" className={styles.menuButton} onClick={() => setModalMode("nav-menu")}><img src={HUD_ICONS.menu} alt="" /> Menu</button></nav></header><section className={`${styles.ranchTitlePanel} ${styles.compactRanchTitlePanel}`}><div><p className={styles.kicker}>M15 Ranch Operations</p><h1>Ranch Hub</h1><p className={styles.message}>{message}</p></div><button type="button" className={styles.ranchInfoButton} onClick={() => setModalMode("ranch-info")} aria-label="Ranch hub details">i</button></section><section className={styles.mapLayer} aria-label="Ranch buildings">{BUILDINGS.map((building) => { const available = isAvailableBuilding(building.id); const selected = selectedBuildingId === building.id; return <button key={building.id} type="button" style={getBuildingStyle(building)} className={`${styles.mapBuilding} ${available ? styles.availableBuilding : styles.lockedBuilding} ${selected ? styles.selectedBuilding : ""}`} onClick={() => handleBuildingClick(building)} aria-label={`${building.title}. ${building.description}`}><img src={building.imageSrc} alt="" /><span className={styles.mapBuildingLabel}>{building.title}</span><span className={styles.mapBuildingBadge}>{building.milestone}</span></button>; })}</section><div className={styles.selectedPanel} aria-live="polite"><strong>{selectedBuilding.title}</strong><span>{selectedBuilding.description}</span></div>{modalMode !== "none" ? <div className={styles.modalBackdrop} role="presentation">
    {modalMode === "nav-menu" ? <section className={`${styles.modalPanel} ${reportStyles.nightModalPanel} ${styles.navMenuPanel}`} role="dialog" aria-modal="true" aria-labelledby="nav-menu-title"><header className={styles.navMenuHeader}><div><p className={styles.kicker}>Ranch Navigation</p><h2 id="nav-menu-title">Menu</h2></div><button type="button" onClick={closeModal}>Close</button></header><div className={styles.navMenuGrid}><button type="button" onClick={() => setModalMode("sleep-confirm")}><img src={HUD_ICONS.sleep} alt="" /><span>Sleep</span><em>Advance the day</em></button><button type="button" onClick={goToRanchJobs}><img src={HUD_ICONS.jobs} alt="" /><span>Ranch Chores</span><em>Assign daily helpers</em></button><button type="button" onClick={goToRanchOffice}><img src={HUD_ICONS.ledger} alt="" /><span>Ranch Office</span><em>Upgrades and repairs</em></button><button type="button" onClick={goToCollection}><img src={HUD_ICONS.collection} alt="" /><span>Collection</span><em>View all creatures</em></button><button type="button" onClick={() => setModalMode("starter-goals")}><img src={HUD_ICONS.goals} alt="" /><span>Goals</span><em>{starterProgress?.completed ?? 0}/{starterProgress?.total ?? starterGoals.length} complete</em></button><button type="button" onClick={goToTown}><img src={HUD_ICONS.home} alt="" /><span>Town</span><em>Market and guild</em></button><button type="button" onClick={goToDevTools}><img src={HUD_ICONS.dev} alt="" /><span>Dev Tools</span><em>Testing utilities</em></button><button type="button" onClick={goToMainMenu}><img src={HUD_ICONS.crest} alt="" /><span>Main Menu</span><em>Save slots</em></button></div></section> : null}
    {modalMode === "ranch-info" ? <section className={`${styles.modalPanel} ${reportStyles.nightModalPanel} ${styles.ranchInfoPanel}`} role="dialog" aria-modal="true" aria-labelledby="ranch-info-title"><header className={styles.navMenuHeader}><div><p className={styles.kicker}>Home Ranch</p><h2 id="ranch-info-title">Ranch Hub</h2></div><button type="button" onClick={closeModal}>Close</button></header><p className={styles.ranchInfoLead}>Select buildings directly on the map. The ranch is your daily home base for care, breeding, nursery progress, chores, upgrades, and travel.</p><div className={styles.ranchInfoStats}><div><span>Status</span><strong>{message}</strong></div><div><span>Feed Stock</span><strong>{feedStock} Feed</strong></div><div><span>Materials</span><strong>{materialsStock}</strong></div><div><span>Condition</span><strong>{ranchCondition} ({ranchDamage}/100)</strong></div><div><span>Penalty</span><strong>{ranchPenaltyLabel}</strong></div></div><p className={styles.ranchInfoNote}>{ranchConditionPenaltySummary}</p>{starterProgress?.nextGoal ? <p className={styles.ranchInfoNote}>Next goal: {starterProgress.nextGoal.label} — {starterProgress.nextGoal.hint}</p> : <p className={styles.ranchInfoNote}>Starter goals complete. Expand your collection or upgrade the ranch.</p>}<p className={styles.ranchInfoNote}>{habitatCounts}</p></section> : null}
    {modalMode === "sleep-confirm" ? <section className={`${styles.modalPanel} ${reportStyles.nightModalPanel} ${reportStyles.sleepPanel}`} role="dialog" aria-modal="true" aria-labelledby="sleep-title"><header className={reportStyles.nightModalHeader}><img className={reportStyles.nightModalIcon} src={HUD_ICONS.sleep} alt="" /><div><p className={reportStyles.reportKicker}>Ranch House</p><h2 id="sleep-title">Sleep Until Tomorrow?</h2><p className={reportStyles.modalLead}>Rest for the night, advance the calendar, process chores, and check the posted monthly tax.</p></div><button type="button" className={reportStyles.infoIconButton} onClick={() => setInfoModalMode("sleep")} aria-label="Sleep details">i</button></header><div className={reportStyles.sleepSummaryGrid}><div><span>Player Energy</span><strong>Food-Based</strong></div><div><span>Condition</span><strong>{ranchCondition}</strong></div><div><span>Penalty</span><strong>{ranchPenaltyLabel}</strong></div><div><span>Posted Tax</span><strong>{taxDue ? `${taxDue} Gold` : "Posting"}</strong></div></div><p className={reportStyles.compactHint}>Security can prevent damage. Field Hauling and Ranch Office repairs can remove condition penalties. Month-end tax is collected after Day 30.</p><div className={styles.modalActions}><button type="button" onClick={closeModal}>Cancel</button><button type="button" className={styles.primaryAction} onClick={handleSleep}>Sleep</button></div></section> : null}
    {modalMode === "day-summary" ? <section className={`${styles.modalPanel} ${reportStyles.nightModalPanel} ${reportStyles.reportModalPanel}`} role="dialog" aria-modal="true" aria-labelledby="summary-title"><header className={reportStyles.nightModalHeader}><div><p className={reportStyles.reportKicker}>Morning Report</p><h2 id="summary-title">New Day Summary</h2><p className={reportStyles.reportDate}>{daySummary?.previousDateLabel} → {daySummary?.nextDateLabel}</p></div><button type="button" className={reportStyles.infoIconButton} onClick={() => setInfoModalMode("day-summary")} aria-label="New day summary details">i</button></header><nav className={reportStyles.reportTabs} aria-label="Morning report pages">{reportPages.map((page) => <button key={page.id} type="button" className={page.id === activeReportPage ? reportStyles.activeReportTab : ""} onClick={() => setReportPage(page.id)}>{page.label}</button>)}</nav><div className={reportStyles.reportPageBody}>{activeReportPage === "overview" ? <section><div className={reportStyles.summaryStatGrid}><div><span>Date</span><strong>{daySummary?.nextDateLabel ?? dateLabel}</strong></div><div><span>Condition</span><strong>{ranchCondition}</strong></div><div><span>Damage</span><strong>{ranchDamage}/100</strong></div><div><span>Penalty</span><strong>{ranchPenaltyLabel}</strong></div></div><p className={reportStyles.compactHint}>{feedingSummary}</p><p className={reportStyles.compactHint}>{ranchConditionPenaltySummary}</p></section> : null}{activeReportPage === "security" ? <section><div className={reportStyles.sectionHeader}><h3>Security Report</h3><span>Score {securityScore} • Danger {securityDangerChance}% • {securitySuccess ? "Success" : securityEventType}</span></div><p className={reportStyles.compactHint}>{securityEventSummary}</p><div className={reportStyles.summaryStatGrid}><div><span>Security Score</span><strong>{securityScore}</strong></div><div><span>Danger Chance</span><strong>{securityDangerChance}%</strong></div><div><span>Result</span><strong>{securitySuccess ? "Success" : securityEventType}</strong></div><div><span>Damage Added</span><strong>+{ranchDamageAdded}</strong></div></div></section> : null}{activeReportPage === "upkeep" ? <section><div className={reportStyles.sectionHeader}><h3>Upkeep + Condition</h3><span>+{ranchDamageAdded} damage • -{ranchDamageRepaired} repaired • Upkeep {upkeepScore}</span></div><p className={reportStyles.compactHint}>{ranchUpkeepSummary}</p><p className={reportStyles.compactHint}>{materialsSummary}</p><div className={reportStyles.summaryStatGrid}><div><span>Materials</span><strong>+{materialsProduced} / {materialsStock}</strong></div><div><span>Upkeep</span><strong>{upkeepScore}</strong></div><div><span>Damage</span><strong>{ranchDamage}/100</strong></div><div><span>Penalty</span><strong>{ranchPenaltyLabel}</strong></div></div></section> : null}{activeReportPage === "chores" ? <section><div className={reportStyles.sectionHeader}><h3>Morning Chore Report</h3><span>{completedChores} helpers completed • +{feedProduced} Feed • +{materialsProduced} Materials</span></div><div className={reportStyles.choreReportGrid}>{groupedResults.map(({ job, results, assignedCreatures, displayCreature, totalFeed, totalMaterials }) => <article key={job.jobId} className={reportStyles.choreReportCard}><img className={reportStyles.choreReportIcon} src={displayCreature ? getCreatureProfilePath(displayCreature) : job.iconPath} alt="" /><div><strong>{job.name}</strong><span>{results.length ? `${results.length} helper${results.length === 1 ? "" : "s"}: ${results.map((result) => result.creatureName).join(", ")}` : assignedCreatures.length ? `${assignedCreatures.length} assigned, no completed effect` : "Unassigned"}</span><em>{results.length ? results.map((result) => result.message).join(" ") : "No chore completed."}</em></div><div className={reportStyles.choreRewardRow}>{results.length ? <>{totalFeed ? <span>+{totalFeed} Feed</span> : null}{totalMaterials ? <span>+{totalMaterials} Materials</span> : null}{job.jobId === "security_patrol" && securityScore ? <span>Security {securityScore}</span> : null}{job.jobId === "comfort_care" && comfortBonus ? <span>+{comfortBonus}% Breeding Comfort</span> : null}{job.jobId === "field_hauling" && upkeepScore ? <span>Upkeep {upkeepScore}</span> : null}<span>-{results.reduce((total, result) => total + result.energyCost, 0)} Energy</span></> : <span>No effect</span>}</div></article>)}</div></section> : null}{activeReportPage === "tax" ? <section><div className={reportStyles.sectionHeader}><h3>{TAX_COLLECTOR.name}</h3><span>{TAX_COLLECTOR.title}</span></div><div className={reportStyles.taxCollectorLayout}><div className={reportStyles.taxCollectorPortraitWrap}><img src={TAX_COLLECTOR.portraitPath} alt={TAX_COLLECTOR.name} /></div><div className={reportStyles.taxCollectorContent}><div className={reportStyles.summaryStatGrid}><div><span>Posted Tax</span><strong>{taxDue} Gold</strong></div><div><span>Due Day</span><strong>Day 30</strong></div><div><span>Days Remaining</span><strong>{taxDaysUntilDue}</strong></div><div><span>Status</span><strong>{taxStatus}</strong></div></div><div className={reportStyles.taxCollectorMessageBox}><p>{taxCollectorMessage}</p></div>{taxStatus === "paid" ? <p className={reportStyles.taxCollectorNote}>Payment was collected successfully. A new posted bill has been set for the new month.</p> : null}{taxStatus === "defaulted" ? <p className={reportStyles.taxCollectorWarning}>Payment failed. The run has ended.</p> : null}</div></div></section> : null}{activeReportPage === "log" ? <section><div className={reportStyles.sectionHeader}><h3>Full Log</h3><span>{nonChoreSummaryItems.length} entries</span></div><p className={reportStyles.compactHint}>{feedingSummary}</p><p className={reportStyles.compactHint}>{securityEventSummary}</p><p className={reportStyles.compactHint}>{ranchConditionPenaltySummary}</p><p className={reportStyles.compactHint}>{ranchUpkeepSummary}</p><p className={reportStyles.compactHint}>{materialsSummary}</p><ul className={reportStyles.reportLogList}>{nonChoreSummaryItems.map((item, index) => <li key={`${index}-${item}`}>{item}</li>)}</ul></section> : null}</div><footer className={reportStyles.reportPager}><button type="button" onClick={() => shiftReportPage(-1)} disabled={reportIndex <= 0}>Back</button><span>Page {reportIndex + 1} / {reportPages.length}</span><button type="button" onClick={() => shiftReportPage(1)} disabled={reportIndex >= reportPages.length - 1}>Next</button><button type="button" className={styles.primaryAction} onClick={() => { closeModal(); setMessage("A new day begins on the ranch."); }}>Start Day</button></footer></section> : null}
    {modalMode === "coming-soon" ? <section className={`${styles.modalPanel} ${reportStyles.nightModalPanel}`} role="dialog" aria-modal="true" aria-labelledby="coming-soon-title"><h2 id="coming-soon-title">{selectedBuilding.title}</h2><p>{selectedBuilding.description}</p><p className={styles.comingSoonText}>{selectedBuilding.actionLabel}</p><div className={styles.modalActions}><button type="button" className={styles.primaryAction} onClick={closeModal}>Close</button></div></section> : null}
    {modalMode === "requests" ? <section className={`${styles.modalPanel} ${reportStyles.nightModalPanel}`} role="dialog" aria-modal="true" aria-labelledby="requests-title"><img className={styles.modalIcon} src={HUD_ICONS.requests} alt="" /><h2 id="requests-title">Requests</h2><p>The full guild contract system now lives in the Town Guild Hall.</p><ul><li>Use Town Road → Guild Hall for contracts.</li><li>Use Ranch Office for ranch infrastructure upgrades.</li><li>Use Ranch Chores for daily assignment rewards.</li></ul><div className={styles.modalActions}><button type="button" className={styles.primaryAction} onClick={closeModal}>Close</button></div></section> : null}
    {modalMode === "starter-goals" ? <section className={`${styles.modalPanel} ${reportStyles.nightModalPanel}`} role="dialog" aria-modal="true" aria-labelledby="goals-title"><img className={styles.modalIcon} src={HUD_ICONS.goals} alt="" /><h2 id="goals-title">Starter Goals</h2><p>{starterProgress?.completed ?? 0} / {starterProgress?.total ?? starterGoals.length} complete. These are optional guideposts, not chores.</p><ul className={styles.goalList}>{starterGoals.map((goal) => <li key={goal.id} className={goal.complete ? styles.goalComplete : ""}><strong>{goal.complete ? "✓" : "○"} {goal.label}</strong><span>{goal.description}</span><em>{goal.hint}</em></li>)}</ul><div className={styles.modalActions}><button type="button" className={styles.primaryAction} onClick={closeModal}>Close</button></div></section> : null}
    {infoModalMode ? <div className={reportStyles.infoOverlay} role="presentation" onMouseDown={() => setInfoModalMode(null)}><section className={reportStyles.infoPanel} role="dialog" aria-modal="true" aria-labelledby="ranch-info-title" onMouseDown={(event) => event.stopPropagation()}><header className={reportStyles.infoPanelHeader}><div><p className={reportStyles.reportKicker}>{infoModalMode === "sleep" ? "Overnight Systems" : "System Log"}</p><h3 id="ranch-info-title">{infoModalMode === "sleep" ? "What Sleep Does" : "Full New Day Details"}</h3></div><button type="button" onClick={() => setInfoModalMode(null)}>Close</button></header>{infoModalMode === "sleep" ? <div className={reportStyles.infoTextBlock}><p>Sleeping advances the day and checks the feed shed before restoring energy.</p><ul><li>Good condition: no sleep recovery penalty.</li><li>Worn condition: -5% sleep recovery.</li><li>Damaged condition: -15% sleep recovery and -1 creature affection.</li><li>Critical condition: -25% sleep recovery and -2 creature affection.</li><li>Production and garden chores can add feed before the feeding check.</li><li>Field Hauling can add materials and automatically repair ranch damage overnight.</li><li>Security may prevent danger events that add ranch damage.</li><li>Month-end tax is projected every night and collected after Day 30.</li></ul></div> : <div className={reportStyles.infoTextBlock}><p>{feedingSummary}</p><p>{securityEventSummary}</p><p>{ranchConditionPenaltySummary}</p><p>{ranchUpkeepSummary}</p><p>{materialsSummary}</p><ul>{nonChoreSummaryItems.map((item, index) => <li key={`${index}-${item}`}>{item}</li>)}</ul></div>}</section></div> : null}
  </div> : null}<footer className={styles.versionFooter}>{version}</footer></section></main>;
}
